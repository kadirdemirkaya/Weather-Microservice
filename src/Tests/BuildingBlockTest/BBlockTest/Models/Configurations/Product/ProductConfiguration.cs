namespace BBlockTest.Models.Configurations.Product
{
    //public class ProductConfiguration : IEntityTypeConfiguration<BBlockTest.Aggregate.Product.Product>
    //{
    //    public void Configure(EntityTypeBuilder<Aggregate.Product.Product> builder)
    //    {
    //        builder.ToTable("Product");

    //        builder.HasKey(p => p.Id);

    //        builder.Property(m => m.Id)
    //            .ValueGeneratedNever()
    //            .HasConversion(
    //                id => id.Id,
    //                value => ProductId.Create(value));

    //        builder.Property(o => o.ProductName);
    //    }
    //}
}
